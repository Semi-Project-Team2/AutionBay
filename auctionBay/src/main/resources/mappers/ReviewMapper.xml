<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.auctionBay.review.model.mapper.ReviewMapper">

	<!-- 받은 후기 목록 조회 -->
	<select id="selectReceivedReviews" parameterType="long" resultType="ReviewDTO">
		select REVIEW_ID, PRODUCT_ID, REVIEWER_NO, REVIEWEE_NO, RATING, CONTENT,
			to_char(CREATED_AT, 'YYYY-MM-DD HH24:MI') as "CREATED_AT_STR"
		from REVIEWS
		where REVIEWEE_NO = #{userNo}
	</select>

	<!-- 보낸 후기 목록 조회 -->
	<select id="selectSentReviews" parameterType="long" resultType="ReviewDTO">
		select REVIEW_ID, PRODUCT_ID, REVIEWER_NO, REVIEWEE_NO, RATING, CONTENT,
			to_char(CREATED_AT, 'YYYY-MM-DD HH24:MI') as "CREATED_AT_STR"
		from REVIEWS
		where REVIEWER_NO = #{userNo}
	</select>
	
	<!-- 후기 작성 -->
	<insert id="insertReview" parameterType="ReviewDTO">
		insert into REVIEWS (PRODUCT_ID, REVIEWER_NO, REVIEWEE_NO, RATING, CONTENT)
		values (#{productId}, #{reviewerNo}, #{revieweeNo}, #{rating}, #{content})
	</insert>

</mapper>